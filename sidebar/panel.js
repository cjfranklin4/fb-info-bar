
// Listen for click on facebook element (nmted by class) and switch active sidebar page
function listenForClicks () {
  document.addEventListener("click", (e) => {

    // CLick on element in FB page (recognized by class name)
    function pageChange(fclass) {

      switch (fclass) {
        case "": //post box class from facebook
          return //update sidebar to postbox.html
      }
    }

  })
}
